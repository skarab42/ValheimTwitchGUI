using UnityEngine.UI;
using UnityEngine;

public class ValheimTwitchGUIScript : MonoBehaviour
{
    [SerializeField]
    public Button mainButton;
    
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log(">>> My Script Start....");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
